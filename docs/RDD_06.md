# ルート天気 — 要件定義書 v0.6（実装引き渡し版）

| 項目 | 内容 |
|---|---|
| 文書番号 | RDD_06（RDD_05 を改訂） |
| 作成日 | 2026-09-05 |
| 対象 | ブルベ仲間向け、コース上の通過時刻ごとの天候予報を表示するブラウザアプリ |
| 状態 | フェーズ1 実装着手（Claude Code へ引き渡し） |
| v0.6 の変更 | データ源を気象庁モデル（MSM／GSM）のみに限定。先行確認の上限を GSM の予報期間である 11 日先までとし、気象庁以外のモデルは使わない |

---

## 1. 目的と背景

- 600 km ブルベ向けに行ったコース×通過時刻の天候解析（風向風速・降水・気温）が実走準備に有用だったため、同等の解析を仲間のランドヌールが自分で行える形にする。
- 既存アプリ Epic Ride Weather（以下 ERW）は有料・スマホ専用で、ブルベ特有の「仮眠を挟む」「夜間走行が長い」「グロス速度で計画する」といった前提が反映されていない。これらをブルベ前提で設計し直す。

## 2. 利用者と利用場面

| 利用者 | 場面 | 求めること |
|---|---|---|
| 出走予定のランドヌール | 出走 1〜3 日前 | どの区間で向かい風・雨・低温に当たるか。仮眠場所と時間の妥当性。装備（レインウェア・防寒）の判断 |
| 同上 | 出走当日〜走行中 | スマホで直近の予報を再計算。遅れに応じて速度・仮眠を修正 |
| 主催側スタッフ（将来） | 開催判断・案内 | コース全体の悪天区間と時間帯の把握 |

- **フェーズ1 の主対象は iPhone（iOS Safari）**とする。多くのランドヌールが Ride with GPS（以下 RwGPS）の iPhone アプリでコースを扱っており、出走直前の確認もスマホで行うため。
- PC ブラウザでも同じ画面が動くことは維持するが、フェーズ1 での動作確認・最適化は iPhone を優先する。Android は フェーズ2 で確認対象に加える。
- 利用者はブルベ経験者を想定し、風向・降水確率などの用語説明は最小限とする。

## 3. 前提・制約

| 項目 | 内容 |
|---|---|
| 対象地域 | 日本国内専用 |
| データ源 | **気象庁の数値予報（MSM／GSM）に限定する（決定）。** 気象庁以外の予報モデルは使わない。したがって予報の上限は GSM の予報期間である 11 日先まで |
| 実行環境 | ブラウザ上で完結する静的 Web アプリ。サーバ側処理を持たない |
| 配布 | GitHub Pages（HTTPS）。フェーズ3 で Cloudflare Workers 等の軽量中継を 1 本追加する可能性あり |
| 利用形態 | 仲間内への無償提供。商用利用しない（データ提供元の無償条件を満たすため） |
| 費用 | サーバ費・API 費ともに無料枠内で運用する |
| 個人データ | コースファイルは端末内で処理し、サーバに送信・保存しない |
| 端末 | iPhone（iOS Safari 最新版）を第一対象。「ファイル」アプリ経由でコースを読み込む |

## 4. データ源

RDD_01 では「気象庁ホームページ API を利用する」としていたが、気象庁ホームページの JSON では中核機能が実現できないため、以下の構成とする。

### 4.1 気象庁ホームページ JSON の位置づけ

- `https://www.jma.go.jp/bosai/forecast/data/forecast/{地域コード}.json` 等は非公式だが安定しており、CORS ヘッダが付与されているためブラウザから直接取得できる。
- ただし内容は府県→一次細分区域単位の天気・6 時間ごとの降水確率・気温、風は「北の風 やや強く」等のテキストのみ。**コース上の任意地点・任意時刻の数値風向風速は取得できない。**
- よって気象庁ホームページ JSON は「注意報・警報」「天気概況テキスト」「アメダス実況」に用途を限定する。

### 4.2 採用するデータ源

| 用途 | データ源 | 内容 | 備考 |
|---|---|---|---|
| コース上の数値予報（主） | Open-Meteo JMA API（`/v1/jma`、モデル `jma_msm`） | 気象庁 MSM の 1 時間刻み・約 5 km 格子。気温・湿度・雲量・降水量・風向風速。4 日先まで | 認証不要、CC BY 4.0、非商用無料。複数地点を 1 リクエストで取得可 |
| 5〜11 日先の予報 | 同上、モデル `jma_gsm`（`forecast_days` 最大 11） | 気象庁 GSM。6 時間刻みを 1 時間に補間。**0.5°（約 55 km）格子** | 出走 5〜11 日前の傾向確認用。精度が落ちる旨を画面に明示。**11 日を超える通過時刻は「予報範囲外」として表示する** |
| 注意報・警報 | 気象庁 `bosai/warning/data/warning/{地域コード}.json` | 府県・市町村単位の発表状況 | コース通過区域に紐付けて表示 |
| 天気概況 | 気象庁 `bosai/forecast/data/overview_forecast/{地域コード}.json` | 府県ごとの概況文 | 参考表示 |
| 実況（走行中） | 気象庁アメダス JSON（`bosai/amedas/`） | 観測所ごとの現在の風向風速・気温・降水 | フェーズ2 |
| 日の出・日の入り | Open-Meteo daily（`sunrise`, `sunset`）または端末内で天文計算 | 夜間帯の判定 | ライブラリ不要で計算可 |
| 標高 | コースファイル内の標高値を優先。欠損時は Open-Meteo Elevation API | 標高プロファイル | |
| 地図タイル（フェーズ2） | OpenStreetMap 標準タイル | 本地図表示の背景 | **決定**。OSM Tile Usage Policy に従い、有効な User-Agent／Referer の付与、タイルの端末内キャッシュ、一括プリフェッチの禁止を守る。利用者が仲間内に限られる規模なら許容範囲。利用が広がった場合は MapTiler 等の無料枠へ切り替え可能な構造にしておく |

### 4.3 出典表示

- 画面下部に「出典：気象庁 数値予報（MSM/GSM）— Open-Meteo 経由」「注意報・警報：気象庁ホームページ」、地図表示時は「© OpenStreetMap contributors」を常時表示する。
- 気象庁ホームページ利用規約、Open-Meteo の CC BY 4.0、OSM の ODbL・タイル利用ポリシーに従う。

### 4.4 負荷への配慮

- 同一コース・同一条件の再計算はブラウザ内キャッシュ（30 分）を使い、再リクエストしない。
- 気象庁サーバへの直接アクセスは注意報・警報・概況に限り、1 コースあたり通過府県数分の GET に留める。

## 5. コースファイルの取得と読み込み（iPhone 前提）

### 5.1 想定する操作手順（フェーズ1）

1. RwGPS iPhone アプリでルートを開き、右上の三点メニューから「Export GPX」を選ぶ
2. iOS の共有シートで「"ファイル"に保存」を選び、任意のフォルダ（例：`ルート天気/`）に保存する
3. Safari で本アプリを開き、「コースを読み込む」をタップ → iOS 標準のファイル選択画面（「ファイル」アプリ）が開く
4. 保存した GPX を選ぶ → 端末内で解析され、コース情報が表示される

- 技術的には `<input type="file">` を用いる。iOS Safari ではこのタップで写真／ファイルの選択肢が出て、「ファイルを選択」から「ファイル」アプリを開ける。**ドラッグ＆ドロップは PC 向けの補助手段とし、モバイルでは前提にしない。**
- iOS は `.gpx` のような非標準拡張子を `accept` 属性で指定すると選択対象が絞られすぎて GPX がグレーアウトする場合があるため、`accept` は緩く指定し（または未指定とし）、読み込み後に内容で判定する。
- iOS Safari は Web Share Target（共有シートから直接 Web アプリへ渡す仕組み）に対応していないため、「共有 → 本アプリ」の直接経路はフェーズ1 では扱わない。上記のとおり「ファイル」アプリ経由の 2 段階とする。
- Android では RwGPS アプリの Export GPX が「Files」アプリに自動保存されるため、同じ手順で読み込める（フェーズ2 で確認）。

### 5.2 POI（ウェイポイント）について

- RwGPS アプリからの GPX エクスポートには POI・カスタムキューが含まれない（座標のみ）。Web サイトからのエクスポートで「Include POI as waypoints」を選べば `<wpt>` として含まれるが、**本アプリでは POI の保持・表示を要件としない（決定）。** GPX 内に `<wpt>` があっても無視し、エラーにはしない。
- PC や仮眠候補地の位置は、利用者が距離（km）を直接入力する。将来 PC 距離を自動で取り込みたい場合はキューシート変換ツールとの連携（フェーズ2 検討）で対応する。

## 6. 機能要件

### 6.1 コース取得

| ID | 要件 | フェーズ |
|---|---|---|
| C-1 | GPX ファイル（`trk`／`rte`）を「ファイル」アプリ（iOS 標準ファイル選択）から読み込む。PC ではドラッグ＆ドロップも受け付ける。緯度経度・標高・累積距離・区間ごとの進行方位を端末内で算出する | 1 |
| C-2 | FIT ファイル（コースファイル）を Garmin FIT SDK（`@garmin/fitsdk`）で端末内デコードする。`record` メッセージの緯度経度・標高・累積距離を用いる | 1 |
| C-3 | 読み込んだコースの名称・総距離・獲得標高・地点数を表示する | 1 |
| C-4 | 読み込み手順（RwGPS アプリからの書き出し→「ファイル」に保存→本アプリで選択）を画面内に短く案内する | 1 |
| C-5 | RwGPS 公開ルートの URL 貼り付けによる取得。CORS 制約のため中継（Cloudflare Workers）を経由する | 3 |
| C-6 | RwGPS OAuth 連携により自分のルート一覧から選択する。トークン交換に中継が必要 | 3 |
| C-7 | コースを反転する（往復・復路確認用） | 2 |
| C-8 | 直近に読み込んだコースを端末内に保持し、再選択できる | 2 |
| C-9 | キューシート変換ツールの出力（PC 距離リスト）を読み込み、区間表に PC 行として挿入する | 2（検討） |

### 6.2 走行計画パラメータ

| ID | 要件 | フェーズ |
|---|---|---|
| P-1 | 出走日・出走時刻を指定する | 1 |
| P-2 | グロス速度（km/h）を指定する。PC・食事等の短い停止はこれに含める前提を画面に明示する | 1 |
| P-3 | 仮眠ポイントを「スタートからの距離（km）」と「仮眠時間（分）」の組で複数登録できる。**指定方法は距離のみとする（決定）**。通過時刻は仮眠時間分だけ後ろにずれる | 1 |
| P-4 | 区間ごとに異なる速度を設定できる（例：山岳区間は 15 km/h）。未設定区間はグロス速度を用いる | 2 |
| P-5 | 走行中に「現在地点（km）と現在時刻」を入力すると、以降の通過時刻を再計算する（遅れ・巻きの反映） | 2 |

### 6.3 予報計算

| ID | 要件 | フェーズ |
|---|---|---|
| F-1 | コースを一定間隔（総距離に応じて 2.5 / 5 / 10 km）でサンプリングし、各地点の通過予定時刻を求める | 1 |
| F-2 | 各サンプル地点の緯度経度を 1 リクエストにまとめて MSM の時系列を取得し、通過時刻に最も近い時刻（必要なら前後補間）の値を採用する | 1 |
| F-3 | 相対風を算出する。風の吹く向きと進行方位の差から「向かい風／横風／追い風」に分類し、進行方向成分（m/s）を求める。閾値：±45° 以内を追い風、±135° 以上を向かい風、その間を横風 | 1 |
| F-4 | 各地点の夜間判定（日没〜日出）を行う | 1 |
| F-5 | 通過時刻が MSM の予報期間（4 日）を超える地点は GSM に切り替え、精度低下を画面に明示する（F-8 に統合） | 1 |
| F-6 | 出走時刻を前後 3 時間（1 時間刻み）ずらした場合の「向かい風区間 km」「雨中走行 km」「最低気温」「ゴール予定」を比較表示する | 1 |
| F-7 | 予報の発表時刻（モデル初期時刻）を表示し、古い場合は再取得を促す | 1 |
| F-8 | **傾向モード（出走 5〜11 日前）**：通過時刻が MSM の 4 日を超える場合、5〜11 日先は `jma_gsm` から取得する。MSM と GSM の 2 リクエストに分け、通過時刻ごとに「MSM があれば MSM、なければ GSM」を採用する。11 日を超える通過時刻は取得せず「予報範囲外」とする | 1 |
| F-9 | 傾向モードでは時間刻みの風矢印を出さず、**コース区間（50 km 単位）×通過日ごと**に「卓越風向と平均風速」「降水量合計」「最高・最低気温」を集約して示す。GSM の値は 6 時間値の補間である旨と、格子の粗さ（約 55 km）を表示に添える | 1 |

### 6.4 表示

| ID | 要件 | フェーズ |
|---|---|---|
| V-1 | **リボン表示**：横軸を距離とし、上から「時刻目盛（日付境界を強調）」「風（相対風の矢印。右＝追い風、左＝向かい風。色で分類、長さで風速）」「雨（降水確率を帯の濃さ、降水量を棒）」「気温（折れ線）」「標高（面）」を縦に積む。夜間帯と仮眠帯は全レーンを貫く帯で示す | 1 |
| V-2 | リボン上のタップ／ホバーで、その地点の距離・通過時刻・天気・気温・標高・風向風速・相対風・降水確率・降水量を表示する。iPhone ではタップで固定表示し、再タップで解除する | 1 |
| V-3 | **要点表示**：ゴール予定時刻、向かい風区間の合計 km、雨中走行の合計 km と開始地点・時刻、最低気温とその地点・時刻を冒頭に表示する | 1 |
| V-4 | **区間表**：一定間隔ごとの通過時刻・天気・風向風速・相対風・降水・気温の一覧 | 1 |
| V-5 | **略地図**：コース形状の上に、風の吹く向き（実方位）を矢印で重ねる。色は相対風分類 | 1 |
| V-6 | OpenStreetMap タイル上にコース・風矢印を重ねる本地図表示（Leaflet） | 2 |
| V-7 | 通過府県の注意報・警報を、該当する距離区間に紐付けて表示する | 2 |
| V-8 | 表示項目の追加：体感温度、湿度、日照時間、UV 指数（ERW 相当）。大気質は日本では優先度低とし任意 | 2 |
| V-9 | 表示結果を画像として書き出し、共有シートから仲間に送れる | 2 |
| V-10 | ダークモード（夜間走行中の閲覧向け） | 2 |
| V-11 | **傾向モードの表示**：出走が 4 日より先の場合はリボンの風レーンを「区間×日」の表（卓越風向・平均風速・降水量・気温）に置き換え、画面上部に「出走まで n 日。予報は傾向としてお読みください（○○ km 格子・□時間刻み）」の帯を出す。出走 4 日前を切ったら通常表示に自動で戻る。出走が 11 日より先の場合は予報を取得せず「出走まで n 日。予報は 11 日前から表示できます」とだけ示す | 1 |

### 6.5 GUI 方針

- ERW（地図中心＋数値表）とは意図的に構成を変え、距離軸リボンを主役に置く。
- 風の矢印は進行方向基準で描き、「右を向いていれば追い風」と直感的に読めるようにする。地図側の矢印のみ実方位とし、凡例で区別を明示する。
- 色は意味を持つものに限定する：向かい風（赤系）、横風（黄系）、追い風（緑系）、降水（青系）、夜間（暗色帯）、仮眠（紫系帯）。装飾目的の色は使わない。
- **iPhone 縦画面を基準に設計する。** 設定欄はリボンの上に折りたたみで置き、リボンは横スクロールさせずレーンの高さを圧縮して 1 画面に収める。区間表は縦スクロールで続ける。
- タップ対象は 44 pt 以上を確保する。

## 7. 非機能要件

| 項目 | 要件 |
|---|---|
| 性能 | 600 km コースの予報計算・描画を、リクエスト 1 往復＋端末内処理 2 秒以内で完了する |
| オフライン | 一度取得した予報はブラウザ内に保持し、圏外でも直前の結果を閲覧できる（PWA 化はフェーズ2。iOS ではホーム画面追加で全画面動作） |
| 対応ブラウザ | フェーズ1：iOS Safari 最新版を必須、PC の Chrome／Safari／Edge 最新版で動作すること。フェーズ2：Android Chrome を追加 |
| アクセシビリティ | キーボード操作可能、フォーカス表示、色だけに頼らない区別（矢印の向き・ラベル併記） |
| 保守性 | 予報プロバイダ（Open-Meteo）・地図タイル（OSM）を差し替え可能なよう、取得層を分離する |
| 依存 | ビルド不要の単一 HTML から始め、必要に応じて Vite 等へ移行する。外部ライブラリは FIT SDK と Leaflet（フェーズ2）のみ |

## 8. フェーズ計画

| フェーズ | 範囲 | 成果物 |
|---|---|---|
| 1 | iPhone の「ファイル」アプリ経由の GPX/FIT 読み込み、Open-Meteo（MSM／GSM）取得、傾向モード（出走 5〜11 日前）、出走時刻・速度・仮眠パラメータ、リボン・要点・区間表・略地図、出走時刻比較、読み込み手順の案内 | GitHub Pages で公開する単一 HTML |
| 2 | 注意報・警報の区間紐付け、アメダス実況、走行中の再計算、区間別速度、キューシート変換ツール連携、OSM 本地図、ダークモード、画像共有、PWA、Android 確認 | 同上（複数ファイル構成） |
| 3 | RwGPS 連携（公開 URL 取得、OAuth） | Cloudflare Workers 1 本を追加 |

## 9. 決定事項（v0.2 の未決事項から）

| 項目 | 決定 |
|---|---|
| 地図タイル | OpenStreetMap 標準タイルを使う。利用ポリシーを守り、規模拡大時は差し替え可能にしておく |
| 仮眠ポイントの指定 | 距離指定のみ |
| フェーズ1 の対象端末 | iPhone（iOS Safari）に限定して最適化。PC は動作維持 |
| コース読み込み経路 | 「ファイル」アプリ経由の 2 段階 |
| POI | 保持・表示しない（GPX 内の `<wpt>` は無視） |
| データ源 | 気象庁の数値予報（MSM／GSM）のみ。気象庁以外のモデルは使わない |
| 先行確認の範囲 | 出走 11 日前まで（GSM の予報期間）。5〜11 日前は「傾向モード」として粒度を落として表示する。11 日より先は予報範囲外 |

## 10. 残る未決事項

- Open-Meteo の非商用条件の解釈：仲間内の無償公開は非商用と判断しているが、主催団体の公式案内に組み込む場合は要確認。
- 相対風分類の閾値（±45° / ±135°）の妥当性：実走後のフィードバックで調整する。

## 11. 参考

- モック画面：`brevet_weather_mock.html`（予報値はダミー、GPX 読み込みは実動作。ドロップ領域は次版で「コースを読み込む」ボタン＋手順案内に置き換える）
- 関連ツール：キューシート変換ツール（PC 距離の取り込み元として連携予定）
- RDD_01（初版要件）、RDD_02（データ源を再構成した版）、RDD_03（iPhone 前提・POI 検討版）、RDD_04（POI 除外）、RDD_05（先行確認を追加）

---

## 付録 A. 実装引き渡しメモ（Claude Code 向け）

要件定義書は「何を作るか」を決める文書なので、実装時に判断が要る事項をここにまとめる。ここに書いていないことは実装側の裁量とする。

### A.1 リポジトリ構成（フェーズ1）

```
route-weather/
├── index.html          # 単一ファイル。ビルド不要
├── mock/
│   └── brevet_weather_mock.html   # 参考：表示イメージ（予報値はダミー）
├── samples/
│   └── *.gpx           # 動作確認用コース（RwGPS アプリからのエクスポートを想定）
├── docs/
│   └── RDD_04.md       # 本書
├── CLAUDE.md
└── .github/workflows/pages.yml   # main へ push で GitHub Pages に公開
```

- フェーズ1 は `index.html` 1 本（HTML + CSS + JS）に収める。JS は責務ごとに関数群で分け、フェーズ2 で ES Modules に分割できるようにしておく。
- FIT SDK は npm 依存を持ち込まず、単一ファイルにビルド済みを同梱するか、`<script type="module">` から CDN（jsDelivr / esm.sh）で読む。CDN 障害時は GPX のみで動くよう FIT 読み込みを遅延ロードにする。

### A.2 モジュール分割（責務）

| 責務 | 内容 | 備考 |
|---|---|---|
| `course` | GPX/FIT の解析、間引き（最大 ~600 点）、累積距離、標高、進行方位 | モックの `loadFile`／`hav`／`bearing` が原型 |
| `plan` | 出走時刻・グロス速度・仮眠から「距離→通過時刻」「経過時間→距離」を求める | モックの `elapsedH`／`distAtElapsed` |
| `forecast` | Open-Meteo への 1 リクエスト、時系列の補間、キャッシュ | プロバイダ差し替え可能なインタフェースにする |
| `wind` | 相対風の分類と進行方向成分 | 閾値は定数化 |
| `sun` | 日出・日没の計算 | 端末内計算（NOAA の簡易式）で可 |
| `view` | 要点・リボン（SVG）・区間表・略地図・出走時刻比較の描画 | モックの `render*` が原型 |
| `ui` | 入力欄、ファイル選択、折りたたみ、タップ固定表示 | |

### A.3 予報取得の技術メモ

- エンドポイント：`https://api.open-meteo.com/v1/jma`
- パラメータ例：
  `latitude=35.68,35.62,...&longitude=139.75,139.60,...&hourly=temperature_2m,relative_humidity_2m,precipitation,weather_code,wind_speed_10m,wind_direction_10m,wind_gusts_10m&models=jma_msm&wind_speed_unit=ms&timezone=Asia%2FTokyo&forecast_days=4`
- 緯度経度をカンマ区切りで複数渡すと、地点ごとの配列で返る。600 km を 10 km 刻みでも 61 地点で 1 リクエスト。
- **確認事項**：JMA モデルで `precipitation_probability` が返らない場合がある。返らなければ降水確率の帯は非表示にし、降水量の棒のみで表現する（要件 V-1 の帯は「取得できる場合」に限定）。
- `wind_direction_10m` は「風の吹いてくる方位」（気象慣例）。相対風の計算では +180° して「吹いていく向き」に直してから進行方位と比較する。
- 4 日を超える通過時刻は次のように取得する（傾向モード、F-8）。
  - 5〜11 日先：`/v1/jma?...&models=jma_gsm&forecast_days=11`（同じ地点リストで 1 リクエスト。1 時間値は 6 時間値の補間である点を画面に明示）
  - 12 日以降：取得しない。通過時刻が GSM の範囲を超える地点は「予報範囲外」として空欄にし、要点表示にその旨を出す
  - 通過時刻ごとに「MSM → GSM」の優先順で、その時刻を含む最も細かいモデルを採用する
  - GSM は約 55 km 格子なので、山岳部の風向は地形の影響を反映しない。傾向モードでは 50 km 区間に集約し、「区間内の卓越風向」として示す
  - GSM は 1 日 2 回（00／12 UTC）更新される。傾向モードでは「予報の発表時刻」を大きめに表示し、半日以上経っていれば再取得を促す
- レスポンスは `sessionStorage`／`localStorage` に「コースのハッシュ＋出走日時＋速度＋仮眠」をキーとして 30 分キャッシュする。
- 気象庁ホームページへの直接アクセスはフェーズ1 では行わない（注意報・警報はフェーズ2）。

### A.4 iOS Safari で確認すべき点

- `<input type="file">` から「ファイル」アプリが開き、RwGPS アプリで保存した `.gpx` が選択できること。`accept` 属性の有無でグレーアウトが変わるかを実機で確認する。
- ホーム画面に追加した状態（standalone）でもファイル選択が動くこと。
- SVG のタップで固定表示ができること（`pointerdown` を使い、`mousemove` には依存しない）。
- 縦画面（幅 375–430 px）でリボン全体が横スクロールなしで収まること。
- `localStorage` の容量とプライベートブラウズ時の例外処理。

### A.5 フェーズ1 の受け入れ条件

1. iPhone Safari で、RwGPS アプリから書き出した 600 km 級 GPX を「ファイル」アプリ経由で読み込める
2. 出走日時・グロス速度・仮眠（複数）を変えると、通過時刻・リボン・区間表・要点・出走時刻比較が即時に更新される
3. 予報値が Open-Meteo（jma_msm）から取得され、モデル初期時刻が画面に表示される
4. 相対風の分類が、進行方位と風向の差から要件どおり（±45°／±135°）に判定される
4a. 出走日を 10 日後に設定すると傾向モードに切り替わり、GSM の値で区間×日の表が表示される。12 日後に設定すると予報を取得せず「予報範囲外」の案内だけが出る
5. 夜間帯と仮眠帯がリボン上で全レーンを貫いて表示される
6. 圏外でも直前の結果が表示される（キャッシュ）
7. GitHub Pages の URL で上記が動作する
8. 出典表示が常時見える

### A.6 作業順（提案）

1. リポジトリ作成、`CLAUDE.md`、GitHub Pages のワークフロー、モックの配置
2. `course`：GPX 解析（モックから移植）、サンプル GPX で距離・標高・方位を検証
3. `plan`・`wind`・`sun`：純粋関数として実装し、ユニットテスト（Node で実行できる形）を付ける
4. `forecast`：Open-Meteo 接続、1 地点→複数地点、キャッシュ。MSM で動いてから GSM へのフォールバックと傾向モードの集約を足す
5. `view`：モックの描画を実データに接続。要点→リボン→区間表→略地図→出走時刻比較の順
6. `ui`：iPhone 縦画面のレイアウト、折りたたみ設定欄、タップ固定表示
7. iPhone 実機確認（A.4）→ 公開
8. FIT 読み込み（最後で可。GPX で全体が動いてから）

### A.7 Claude Code と Codex の分担（参考）

- Claude Code：設計・実装・GitHub Pages 公開までを担当する。
- Codex（Claude Code 配下）：`plan`／`wind`／`sun` の純粋関数のレビューとテストケース生成、Open-Meteo レスポンス形式の変化に対する堅牢性チェックなど、**独立した目で検算させたい箇所**に限定して使う。UI の実装を二重に任せると方針がぶれるので避ける。
