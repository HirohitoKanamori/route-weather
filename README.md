# Route-Weather.jp（route-weather）

公開版 v1.1.1（2026-09-07、Beta）。バージョンは `index.html` フッターの `#appVersion` を更新し、`git tag vX.Y.Z` を打つ。

旧称「ルート天気」。

ブルベ（長距離自転車）向け。コース（GPX / FIT）と出走時刻・移動速度・仮眠から、コース上の通過時刻ごとの風（相対風）・降水・気温を表示するブラウザアプリ。

- 予報は気象庁の数値予報（MSM：4 日先まで、GSM：11 日先まで）を Open-Meteo 経由で取得
- ビルド不要の静的ファイル（HTML ＋ ES Modules）。GitHub Pages で公開
- コースファイルは端末内で処理し、外部に送信しない
- 第一対象は iPhone Safari（「ファイル」アプリ経由で GPX を読み込む）

要件は [docs/RDD_06.md](docs/RDD_06.md)、実装方針は [CLAUDE.md](CLAUDE.md)。

## 使い方

1. Ride with GPS アプリでルートを開き「⋯」→ Export GPX →「"ファイル"に保存」
2. 本アプリを開き「コースを読み込む」で保存した GPX を選ぶ
3. 出走日時・移動速度・仮眠（距離 km と分）を入力する
4. 通過時刻ごとの風・雨・気温がリボン・区間表・略地図に表示される

出走が 4 日より先の場合は「傾向モード」（GSM、50 km 区間 × 通過日で集約）、11 日より先は予報範囲外。

### フェーズ2 で追加した機能

- 区間別速度（山岳区間だけ遅くする等）、走行中の再計算（現在地点 km と現在時刻を入れると以降を計算し直す）
- コース反転、最近のコース（端末内に 5 件）
- 通過区域の注意報・警報（気象庁）、アメダス実況（現在地から 50 km ごとの最寄り観測所）
- 体感温度・湿度・日照、画像で共有（共有シート／保存）、ダークモード、OpenStreetMap の本地図（Leaflet、モノクロ表示。矢印タップで詳細）
- PWA（ホーム画面に追加すると全画面。一度開いた本体・FIT SDK・地図タイルは圏外でも表示）
- v1.1.0（試験運用中）：「現在位置から予報を再取得」ボタン。端末の GPS と時刻を取得してコース上の距離に変換し、走行中の再計算（P-5）を自動で行う。現在地より先の予報だけ取り直し、要点は「残り」ベース、通過済み区間は薄く表示。往復コースで候補が複数なら選択、±2 km 先の仮眠は確認。仕様は `docs/ADD_01.md`
- テスター意見：地図の経路線を通過時刻の予報で色分け（雨＝青、曇り＝グレー、晴れ＝オレンジ、予報範囲外＝点線）、コース全体の地図を 1080×1080 の画像にして共有（「地図を画像で共有」）

## 変更履歴（主なもの）

- v1.1.6（未公開・確認待ち）: Codex レビュー（2026-09-10）の 9 件を修正。警報の取得失敗・判定不能を「なし」と区別、予報取得中の設定・コース変更で古い応答を破棄して最後の設定で取り直す、コース変更で現在地の固定を解除、比較表のおすすめは全区間に予報がある候補だけ、距離集計をサンプル間の実距離に（現在地・ゴールで切り詰め）、傾向表の降水量は区間別速度を反映した走行時間で積算、重なった速度区間の除外を採用済み末尾基準に、欠測を 0 にせず「—」表示、表示モードの保存形式を統一
- v1.1.5（2026-09-09）: 区間表と「出走時刻をずらした場合」を折りたたみ（初期は閉じる）に。雨中走行の「最初／最後」の語を除去。傾向表・比較表の見出しを「向かい風区間距離 km」に、夜間行をグレーに
- v1.1.4（2026-09-08）: アイコンを新デザインに差し替え（`icons/ride-weather-icons/` が元データ。favicon と PWA アイコン、ホーム画面アイコン）。地図の経路線の色分けで、降水 0.5 mm/h 未満は雨系の天気コードでも曇り扱いに修正
- v1.1.3（2026-09-08）: 出走日・出走時刻を変更したら、隠れていた現在位置の固定（走行中の再計算）を自動で解除して帯で知らせる。固定が残ったままだと出走時刻を変えても以降の予報が更新されない不具合への対処
- v1.1.2（2026-09-08）: 出走日時の初期値を「端末時刻より先にある最も近い 06:00」に変更。出走日時は**4 日前の 0:00（JST）より過去に設定できない**（1200 km・制限 90 時間の走行中でも実際の出走時刻を入れられるよう緩和。予報の取得も過去 5 日分まで）。下限より前の保存値は開いた時点で初期値に戻る。出走日時が数日前だと通過時刻が予報の取得範囲（過去 2 日）から外れて「予報範囲外」になる不具合への対処。戻す場合は `git revert` するか、`js/core.js` の `minStart` の下限と `js/app.js` の `enforceStart` / `loadParams` を変える（タグ `v1.1.1` が変更前）
- v1.1.1: 設定欄の「走行中の再計算」を非表示にし、位置取得失敗時のみキロ数入力欄を表示
- v1.1.0: 「現在位置から予報を再取得（試験運用中）」（docs/ADD_01.md）
- v1.0.x: 公開・テスター対応（名称変更、地図の色分け・共有画像、警報の 2026 年改正対応、バッジ色）

## 開発

- 純粋関数（course / plan / wind / sun / forecast）は `js/core.js`（ES Module）にまとめ、`test/` が直接 import する。画面と取得層は `js/app.js`
- テスト：`node --test "test/*.test.mjs"`
- ローカル確認：リポジトリ直下で `python3 -m http.server 8000` → `http://localhost:8000/`（`file://` ではサンプル読み込みと FIT SDK の import が動かない）
- 公開：`main` へ push すると `.github/workflows/pages.yml` がテストを実行してから GitHub Pages に配置する（リポジトリ設定の Pages → Source を「GitHub Actions」にしておく）

## 構成

```
index.html                  本体（HTML / CSS）
js/core.js, js/app.js       純粋関数（ES Module）と画面・取得層
sw.js, manifest.webmanifest, icons/  PWA 用（sw.js は vendor 更新時に VERSION を上げる）
vendor/fitsdk/              Garmin FIT SDK（@garmin/fitsdk の src をそのまま同梱、遅延 import）
vendor/leaflet/             Leaflet 1.9.4（本地図）
samples/*.gpx               動作確認用コース
mock/                       表示イメージのモック
docs/RDD_06.md              要件定義書
test/                       Node テスト
.github/workflows/pages.yml GitHub Pages デプロイ
```

## 出典

気象庁 数値予報（MSM／GSM）— [Open-Meteo](https://open-meteo.com/)（CC BY 4.0）経由。注意報・警報とアメダスは気象庁ホームページ、市区町村判定は国土地理院 逆ジオコーダ、地図は © OpenStreetMap contributors（ODbL）。予報は参考情報です。
